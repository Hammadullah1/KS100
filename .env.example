# Optional, server-side only. Never use VITE_ variables for credentials.
EIA_API_KEY=
FRED_API_KEY=
# Set in GitHub secrets only after private-state access is configured.
# STATE_TOKEN: fine-grained token scoped only to the private state repository
